#include <fstream> #include <iostream> #include <string> #include <sstream>#include <iomanip>using namespace std; class Search {    public:		void BinSearch(int item, int length, bool& found, float& comp);		void sequintialSearchPass(float& comp, int compare);		void sequintialSearchFail(float& comp, int compareFail);		void  BubbleSort  (int numValues);		void BubbleUp  (int  start ,  int end );		void readRd (ifstream& inFile);		void hashingSearch(int compare[], int compareFail[], int hashing[],  ofstream& outFile, float& avg, float& comp, float&    hashfail, float& hashpass );		void doublehashingSearch(int compare[], int compareFail[], int doubleHashing[], ofstream& outFile, float& avg, float& comp,float&   dhashfail, float& dhashpass );		void sequentialList (int compare[]);		void failList (int compareFail[]); 		private: 		int data[40];	int list[20];	};	