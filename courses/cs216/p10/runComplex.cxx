//**************************************************************************** // Author : Michael Gutierrez// Course Title : Computer Programming II // Course Number : CS216// Class Meets : M W / 8:30 - 10:20// Prof Name : Moe Bidgoli// Assignment Number : #10// Due Date : 11-23-15// Possible Points : 20// Purpose :// This Program read computer data in from a file and applys functions and outputs // using overloaded operators.//**************************************************************************** #include <fstream> #include <iostream> #include <string> #include <iomanip> #include "Complex.h"using namespace std; int main() {      ifstream inFile;    ofstream outFile;        inFile.open("in.data");    outFile.open("out.data");        if(inFile.fail() || outFile.fail() )    {        cout << "Input file or output file opening failed" << endl;        return 1;    }	outFile << "<* Complex Numbers *>" << endl << endl;		Complex A, B, Temp1, Temp2;		inFile >> A >> B;		outFile << "A = ";    outFile << A << endl;	outFile << "B = ";	outFile << B << endl;		outFile << "A + B = " ;	Temp1 = A;	Temp2 = B;	outFile << (Temp1+Temp2) << endl;		outFile << "A - B = ";	Temp1 = A;	Temp2 = B;    outFile	<< (Temp1-Temp2) << endl;		outFile << "A * B = ";	Temp1 = A;	Temp2 = B;    outFile	<< (Temp1 * Temp2) << endl;		Temp1 = A;	Temp2 = B;	if (B.O1is() == 0 && B.O2is() == 0)	{	  outFile << "B = "	;	  outFile << B << "    Can not Divide!" << endl << endl;	}	else	{	  outFile << "A / B = ";      outFile << (Temp1/Temp2) << endl;	}		outFile << "++A = ";	Temp1 = A;	Temp2 = B;    outFile	<< (++Temp1) << endl;		outFile << "B++ = ";	Temp1 = A;	Temp2 = B;    outFile << (Temp2++) << endl;		outFile << endl << "<End>"; }	